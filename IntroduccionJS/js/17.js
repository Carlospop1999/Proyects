const num1 = 20;
const num2 = "20";

// Diferencias una funcion entre un método

console.log(parseInt(num2)); // parseInt() es una función porque la variable sigue despues del parentesis
console.log(num1.toString()); // .toString es un método porque la variable esta antes de un punto (.) 