//Objeto MATH *propiedades*

let resultado;

resultado = Math.PI;
resultado = Math.round(4.5); //Redondea a partir del .5
resultado = Math.ceil(2.1); //Redondea el número hacia arriba en lugar de 2 se redondea a 3
resultado = Math.floor(2.9); //Redondia el número hacia abajo en lugar de 3 se redondea a 2
resultado = Math.sqrt(81); //Saca la raiz cuadra de dicho número
resultado = Math.abs(-900); //Cambia de signo negativo el número para que siempre sea positivo
resultado = Math.min(4, 7, 9, 2, 6); //Elige de una lista el número mas pequeño de valor
resultado = Math.max(4, 7, 9, 2, 6); //Elige de una lista el número mas grande de valor
resultado = Math.random(); //Elige un número random
resultado = Math.floor(Math.random() * 30); //Se puede combinar varias funciones para completar el objetivo

console.log(resultado);